import { ether } from "@utils/index";

export const CONTRACT_NAMES = {
  BASE_MANAGER_NAME: "BaseManager - BED",
  GIM_EXTENSION_NAME: "GIMExtension - BED",
  FEE_EXTENSION_NAME: "StreamingFeeSplitExtension - BED",
};

export const FEE_SPLIT_ADAPTER = {
  FEE_SPLIT: ether(.5),                       // 100% operator, 0% methodologist fee split
};